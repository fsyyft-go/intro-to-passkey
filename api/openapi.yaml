# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: ""
    version: 0.0.1
paths:
    /helloworld/{name}:
        get:
            tags:
                - Greeter
            description: Sends a greeting
            operationId: Greeter_SayHello
            parameters:
                - name: name
                  in: path
                  required: true
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/HelloReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /user/change-password:
        post:
            tags:
                - User
            description: ChangePassword 修改密码。
            operationId: User_ChangePassword
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/ChangePasswordRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/ChangePasswordResponse'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /user/login:
        post:
            tags:
                - User
            description: Login 用户登录。
            operationId: User_Login
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/LoginRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/LoginResponse'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /user/logout:
        post:
            tags:
                - User
            description: Logout 用户退出登录。
            operationId: User_Logout
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/LogoutRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/LogoutResponse'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /user/register:
        post:
            tags:
                - User
            description: Register 用户注册。
            operationId: User_Register
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/RegisterRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/RegisterResponse'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
components:
    schemas:
        ChangePasswordRequest:
            type: object
            properties:
                oldPassword:
                    type: string
                    description: 旧密码。
                newPassword:
                    type: string
                    description: 新密码。
            description: ChangePasswordRequest 修改密码请求。
        ChangePasswordResponse:
            type: object
            properties:
                success:
                    type: boolean
                    description: 是否成功。
            description: ChangePasswordResponse 修改密码响应。
        GoogleProtobufAny:
            type: object
            properties:
                '@type':
                    type: string
                    description: The type of the serialized message.
            additionalProperties: true
            description: Contains an arbitrary serialized message along with a @type that describes the type of the serialized message.
        HelloReply:
            type: object
            properties:
                message:
                    type: string
            description: The response message containing the greetings
        LoginRequest:
            type: object
            properties:
                username:
                    type: string
                    description: 用户名或邮箱。
                password:
                    type: string
                    description: 密码。
            description: LoginRequest 用户登录请求。
        LoginResponse:
            type: object
            properties:
                userId:
                    type: string
                    description: 用户 ID。
                username:
                    type: string
                    description: 用户名。
                accessToken:
                    type: string
                    description: 访问令牌。
                tokenType:
                    type: string
                    description: 令牌类型，例如 "Bearer"。
                expiresIn:
                    type: string
                    description: 过期时间（秒）。
            description: LoginResponse 用户登录响应。
        LogoutRequest:
            type: object
            properties: {}
            description: LogoutRequest 退出登录请求。
        LogoutResponse:
            type: object
            properties:
                success:
                    type: boolean
                    description: 是否成功。
            description: LogoutResponse 退出登录响应。
        RegisterRequest:
            type: object
            properties:
                username:
                    type: string
                    description: 用户名，3-32 个字符。
                password:
                    type: string
                    description: 密码，8-32 个字符。
                email:
                    type: string
                    description: 电子邮件地址。
            description: RegisterRequest 用户注册请求。
        RegisterResponse:
            type: object
            properties:
                userId:
                    type: string
                    description: 用户 ID。
                username:
                    type: string
                    description: 用户名。
            description: RegisterResponse 用户注册响应。
        Status:
            type: object
            properties:
                code:
                    type: integer
                    description: The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code].
                    format: int32
                message:
                    type: string
                    description: A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client.
                details:
                    type: array
                    items:
                        $ref: '#/components/schemas/GoogleProtobufAny'
                    description: A list of messages that carry the error details.  There is a common set of message types for APIs to use.
            description: 'The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors).'
tags:
    - name: Greeter
      description: The greeting service definition.
    - name: User
      description: User 定义用户相关的服务接口。
